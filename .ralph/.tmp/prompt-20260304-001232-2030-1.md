# Build

You are an autonomous coding agent. Your task is to complete the work for exactly one story and record the outcome.

## Paths
- PRD: H:/mygames/improv guesser/.agents/tasks/prd-jobs-web-refactor.json
- AGENTS (optional): H:/mygames/improv guesser/AGENTS.md
- Progress Log: H:/mygames/improv guesser/.ralph/progress.md
- Guardrails: H:/mygames/improv guesser/.ralph/guardrails.md
- Guardrails Reference: H:/NVM/nvm/v18.18.2/node_modules/@iannuttall/ralph/.agents/ralph/references/GUARDRAILS.md
- Context Reference: H:/NVM/nvm/v18.18.2/node_modules/@iannuttall/ralph/.agents/ralph/references/CONTEXT_ENGINEERING.md
- Errors Log: H:/mygames/improv guesser/.ralph/errors.log
- Activity Log: H:/mygames/improv guesser/.ralph/activity.log
- Activity Logger: H:/NVM/nvm/v18.18.2/node_modules/@iannuttall/ralph/.agents/ralph/log-activity.sh
- No-commit: false
- Repo Root: H:/mygames/improv guesser
- Run ID: 20260304-001232-2030
- Iteration: 1
- Run Log: H:/mygames/improv guesser/.ralph/runs/run-20260304-001232-2030-iter-1.log
- Run Summary: H:/mygames/improv guesser/.ralph/runs/run-20260304-001232-2030-iter-1.md

## Global Quality Gates (apply to every story)
- npm run build

## Selected Story (Do not change scope)
ID: US-002
Title: Build setup flow for players and situation selection

Story details:
### US-002: Build setup flow for players and situation selection
Status: in_progress
Depends on: US-001

Description:
As a host, I want to enter player names and choose a specific or random situation so I can start a valid round quickly.

Acceptance Criteria:
- [ ] Provide input rows for player names with add/remove controls and enforce minimum 4 players.
- [ ] Provide a situation selector with a Random option that chooses only compatible situations.
- [ ] Display setup status including ready player count, minimum required, and max supported for selected situation.
- [ ] Example: entering 4 unique names and choosing Random enables starting a round.
- [ ] Negative case: attempting to start with duplicate names like 'Alex' and 'alex' shows a validation error and blocks start.


If the story details are empty or missing, STOP and report that the PRD story format could not be parsed.

## Rules (Non-Negotiable)
- Implement **only** the work required to complete the selected story.
- Complete all tasks associated with this story (and only this story).
- Do NOT ask the user questions.
- Do NOT change unrelated code.
- Do NOT assume something is unimplemented — confirm by reading code.
- Implement completely; no placeholders or stubs.
- If No-commit is true, do NOT commit or push changes.
- Do NOT edit the PRD JSON (status is handled by the loop).
- All changes made during the run must be committed (including updates to progress/logs).
 - Before committing, perform a final **security**, **performance**, and **regression** review of your changes.

## Your Task (Do this in order)
1. Read H:/mygames/improv guesser/.ralph/guardrails.md before any code changes.
2. Read H:/mygames/improv guesser/.ralph/errors.log for repeated failures to avoid.
3. Read H:/mygames/improv guesser/.agents/tasks/prd-jobs-web-refactor.json for global context (do not edit).
4. Fully audit and read all necessary files to understand the task end-to-end before implementing. Do not assume missing functionality.
5. If H:/mygames/improv guesser/AGENTS.md exists, follow its build/test instructions.
6. Implement only the tasks that belong to US-002.
7. Run verification commands listed in the story, the global quality gates, and in H:/mygames/improv guesser/AGENTS.md (if required).
8. If the project has a build or dev workflow, run what applies:
   - Build step (e.g., `npm run build`) if defined.
   - Dev server (e.g., `npm run dev`, `wrangler dev`) if it is the normal validation path.
   - Confirm no runtime/build errors in the console.
9. Perform a brief audit before committing:
   - **Security:** check for obvious vulnerabilities or unsafe handling introduced by your changes.
   - **Performance:** check for avoidable regressions (extra queries, heavy loops, unnecessary re-renders).
   - **Regression:** verify existing behavior that could be impacted still works.
10. If No-commit is false, commit changes using the `$commit` skill.
    - Stage everything: `git add -A`
    - Confirm a clean working tree after commit: `git status --porcelain` should be empty.
    - After committing, capture the commit hash and subject using:
      `git show -s --format="%h %s" HEAD`.
11. Append a progress entry to H:/mygames/improv guesser/.ralph/progress.md with run/commit/test details (format below).
    If No-commit is true, skip committing and note it in the progress entry.

## Progress Entry Format (Append Only)
```
## [Date/Time] - US-002: Build setup flow for players and situation selection
Thread: [codex exec session id if available, otherwise leave blank]
Run: 20260304-001232-2030 (iteration 1)
Run log: H:/mygames/improv guesser/.ralph/runs/run-20260304-001232-2030-iter-1.log
Run summary: H:/mygames/improv guesser/.ralph/runs/run-20260304-001232-2030-iter-1.md
- Guardrails reviewed: yes
- No-commit run: false
- Commit: <hash> <subject> (or `none` + reason)
- Post-commit status: `clean` or list remaining files
- Verification:
  - Command: <exact command> -> PASS/FAIL
  - Command: <exact command> -> PASS/FAIL
- Files changed:
  - <file path>
  - <file path>
- What was implemented
- **Learnings for future iterations:**
  - Patterns discovered
  - Gotchas encountered
  - Useful context
---
```

## Completion Signal
Only output the completion signal when the **selected story** is fully complete and verified.
When the selected story is complete, output:
<promise>COMPLETE</promise>

Otherwise, end normally without the signal.

## Additional Guardrails
- When authoring documentation, capture the why (tests + implementation intent).
- If you learn how to run/build/test the project, update H:/mygames/improv guesser/AGENTS.md briefly (operational only).
- Keep AGENTS operational only; progress notes belong in H:/mygames/improv guesser/.ralph/progress.md.
- If you hit repeated errors, log them in H:/mygames/improv guesser/.ralph/errors.log and add a Sign to H:/mygames/improv guesser/.ralph/guardrails.md using H:/NVM/nvm/v18.18.2/node_modules/@iannuttall/ralph/.agents/ralph/references/GUARDRAILS.md as the template.

## Activity Logging (Required)
Log major actions to H:/mygames/improv guesser/.ralph/activity.log using the helper:
```
H:/NVM/nvm/v18.18.2/node_modules/@iannuttall/ralph/.agents/ralph/log-activity.sh "message"
```
Log at least:
- Start of work on the story
- After major code changes
- After tests/verification
- After updating progress log

## Browser Testing (Required for Frontend Stories)
If the selected story changes UI, you MUST verify it in the browser:
1. Load the `dev-browser` skill.
2. Navigate to the relevant page.
3. Verify the UI changes work as expected.
4. Take a screenshot if helpful for the progress log.

A frontend story is NOT complete until browser verification passes.
