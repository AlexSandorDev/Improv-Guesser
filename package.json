{
  "name": "improv-guesser",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "sync:situations": "node scripts/sync-situations.mjs",
    "dev": "npm run sync:situations && vite",
    "build": "npm run sync:situations && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.2",
    "vite": "^5.4.10"
  }
}
